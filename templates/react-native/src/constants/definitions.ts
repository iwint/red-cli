import {
    APP_BASE_URL,
    APP_GOOGLE_CLIENT_ID,
} from '@env';

const BASE_URL = APP_BASE_URL || '';
const GOOGLE_CLIENT_ID = APP_GOOGLE_CLIENT_ID || '';

export {
    BASE_URL,
    GOOGLE_CLIENT_ID,
};
